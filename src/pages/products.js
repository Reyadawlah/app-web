import React from "react";
import { Container, Typography } from "@mui/material";

const ProductsPage = () => {
  return (
    <Container>
      <Typography variant="h2">Travel App</Typography>
      <Typography variant="body1">Details about Travel App</Typography>
    </Container>
  );
};

export default ProductsPage;